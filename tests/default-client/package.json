{
  "name": "test-project",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma-flare generate",
    "test": "vitest run",
    "benchmark": "vitest run tests/benchmark.test.ts --config vitest.benchmark.config.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "prisma": "^5.10.0",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
